<html>

<head>
    <style>
        table {
            font-family: Arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        td,
        th {
            border: 1px solid #cccccc;
            text-align: left;
            padding: 8px;
        }
    </style>
</head>

<body>
    <h2 style="text-align: center;">Laka Cafe</h2>
    <table>
        <tr>
            <th>Tên người dùng: <%= name %>
            </th>
            <th>Email: <%= email %>
            </th>
        </tr>

        <tr>
            <th>Số điện thoại: <%= contactNumber %>
            </th>
            <th>Phương thức thanh toán: <%= paymentMethod %>
            </th>
        </tr>
    </table>

    <h3>Chi tiết sản phẩm: </h3>
    <table>
        <tr>
            <th>Tên</th>
            <th>Danh mục</th>
            <th>Số lượng</th>
            <th>Giá</th>
            <th>Thành tiền</th>
        </tr>

        <% if(productDetails.length > 0) { %>
            <% productDetails.forEach(product => { %>
                <tr>
                    <td>
                        <%= product.name %>
                    </td>
                    <td>
                        <%= product.category %>
                    </td>
                    <td>
                        <%= product.quantity %>
                    </td>
                    <td>
                        <%= product.price %>
                    </td>
                    <td>
                        <%= product.total %>
                    </td>
                </tr>
                <%});%>
                    <%}%>
    </table>
    <h3>Tổng tiền: <%= totalAmount %>
    </h3>
    <h3>Cảm ơn bạn đã sử dụng dịch vụ. Chúc quý khách một ngày tốt lành!!</h3>
</body>

</html>